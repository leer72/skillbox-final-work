{% extends 'dashboard/base.html.twig' %}
{% block body %}

<div class="container-fluid">

    <div class="row">

        <div class="col-xl-6">

            <h1 class="mt-4">{{ article.title | raw }}</h1>
            {{ article.body | raw }}
    </div>
    
</div>

<hr>

<div class="row my-4">

    <div class="col-xl-6">

    <a href="{{ path('app_create_article', { id: article.id }) }}" class="btn btn-primary">Попробовать еще раз</a>
    </div>
    
</div>
        
{% endblock %}
